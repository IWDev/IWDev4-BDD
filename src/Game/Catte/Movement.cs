namespace Catte
{
    /// <summary>
    /// A simple enum type for representing player movement.
    /// </summary>
    public enum Movement
    {
        None,
        Up,
        Right,
        Down,
        Left
    }
}
